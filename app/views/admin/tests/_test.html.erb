<tr>
  <td><%= link_to test.title, admin_test_path(test) %></td>
  <td><%= test.category.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.size %></td>
  <td><%= link_to t('.change'), edit_admin_test_path(test) %></td>
  <td><%= link_to t('.delete'), admin_test_path(test), method: :delete, data: {confirm: t('.del_confirm', title: test.title)} %></td>
</tr>
