<h1>Прохождение теста: <%= @test_passage.test.title %></h1>
<b>
  <%= @test_passage.current_question.body %>
</b>

<%= form_with url: test_passage_path(@test_passage), local: true, method: :put do |form| %>
  <%= form.collection_check_boxes :answer_ids, @test_passage.current_question.answers, :id, :body, include_hidden: false do |b| %>
  <p>
    <%= b.check_box id: "answer_ids_#{b.object.id}" %>
    <%= b.label %>
  </p>
  <% end %>
  <p><%= form.submit 'Далее' %></p>
  <p>Вопрос <%= @test_passage.current_question_num %> из <%= @test_passage.total_questions_in_test %></p>
  <p>Прогресс:<br><%= @test_passage.show_progress_bar %></p>
<% end %>
<p><%= link_to 'Вернуться к списку тестов', tests_path %></p>
