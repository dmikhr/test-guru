<h1>Users</h1>
<ul>
  <% @users.each do |user| %>
  <li><%= link_to user.full_name, user_path(user) %></li>
  <!-- такой вариант приводит к ошибке Missing partial admins/_admin... -->
  <%#= render @users %>
  <!-- такой вариант тоже не решает проблемы -->
  <%#= render 'users/user', collection: @users, as: :user %>
  <% end %>
</ul>
<%= link_to 'Back', root_path %>
